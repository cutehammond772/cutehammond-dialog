import { css } from "@linaria/core";
import * as props from "@theme/properties";

export default css`
  ${props.background}: #202020;
  ${props.header}: black;
  ${props.footer}: black;
  ${props.text}: #b0b0b0;
`;
