import { css } from "@linaria/core";
import * as props from "@theme/properties";

export default css`
  ${props.background}: #f0f0f0;
  ${props.header}: #b0b0b0;
  ${props.footer}: #b0b0b0;
  ${props.text}: black;
`;
