import { registerTheme } from "@theme/store";

import light from "@theme/templates/light";
import dark from "@theme/templates/dark";

export const LIGHT = registerTheme(light);
export const DARK = registerTheme(dark);
